# SOSO All-In-One Scheduler 성능 최적화 포트폴리오 보고서

> 최적화 일자: 2026-08-13
> 대상 프로젝트: SOSO All-In-One Scheduler (React 19 + Spring Boot 4 + MySQL)

---

## 1. 프로젝트 개요

| 구분       | 기술 스택                                                                                                       |
| ---------- | --------------------------------------------------------------------------------------------------------------- |
| 프론트엔드 | React 19, TypeScript, Vite 8, Tailwind CSS 4, TanStack React Query 5, Zustand 5, React Router 7, FullCalendar 6 |
| 백엔드     | Spring Boot 4, Spring Data JPA, Spring Security, JWT, Flyway, MySQL                                             |
| 인프라     | Docker, Nginx, HikariCP                                                                                         |

**핵심 기능**: ToDo 관리, 캘린더 일정 관리, 친구 관계, 다인 일정 조율(최적 시간대 추천)

---

## 2. 기존 구현의 장점 (잘 구현된 부분)

### 프론트엔드

| 항목                             | 설명                                                                                                  |
| -------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **React Query 기반 데이터 캐싱** | `staleTime: 30s`, `refetchOnWindowFocus: false`로 불필요한 재요청 차단. 쿼리 키 기반 자동 캐시 무효화 |
| **Optimistic Update 패턴**       | TodoPage에서 토글/삭제 시 서버 응답 대기 없이 UI 즉시 반영 (`onMutate`로 캐시 선업데이트)             |
| **Zustand 경량 상태 관리**       | Context API 대비 불필요 리렌더 최소화, 인증 상태를 전역으로 효율적으로 관리                           |
| **Axios 토큰 리프레시 인터셉터** | 401 발생 시 자동 리프레시 + 원본 요청 재시도, 동시 요청 큐잉으로 중복 리프레시 방지                   |
| **Promise.all 병렬 요청**        | FriendsPage에서 친구 목록/요청 목록을 병렬 fetch로 워터폴 제거                                        |
| **JPA LAZY 로딩 기본 적용**      | 모든 `@ManyToOne` 관계가 `FetchType.LAZY`로 설정되어 불필요한 즉시 로딩 방지                          |

### 백엔드

| 항목                         | 설명                                                 |
| ---------------------------- | ---------------------------------------------------- |
| **Docker 멀티스테이지 빌드** | 빌드(jdk) ↔ 런타임(jre) 분리로 이미지 크기 최소화    |
| **RefreshToken 해시 저장**   | 평문 토큰이 아닌 SHA-256 해시로 저장하여 보안성 확보 |
| **세션별 토큰 관리**         | 기기별 RefreshToken 추적, 최대 동시 세션 제한(5개)   |

---

## 3. 적용한 최적화 항목 및 정량적 성과

### 3.1 프론트엔드 번들 최적화 (코드 스플리팅 + Lazy Loading)

**개선 내용:**

- `vite.config.ts`에 `manualChunks` 함수 추가: react-vendor, calendar-vendor, query-vendor, ui-vendor 분할
- `router.tsx`에 `React.lazy()` + `<Suspense>` 적용: 4개 페이지 지연 로딩

**정량적 성과 (Vite 프로덕션 빌드 결과 직접 비교):**

#### 전체 번들 구조 비교

| 지표                          | 개선 전         | 개선 후     | 개선 효과                  |
| ----------------------------- | --------------- | ----------- | -------------------------- |
| **JS 번들 개수**              | 1개 (단일 파일) | 14개 (분할) | 청크 캐시 효율화           |
| **전체 다운로드 크기 (raw)**  | 725.06 kB       | 728.07 kB   | +0.4% (청크 래퍼 오버헤드) |
| **전체 다운로드 크기 (gzip)** | 219.09 kB       | 224.46 kB   | +2.4% (청크 래퍼 오버헤드) |
| **최대 청크 크기 (raw)**      | 692.17 kB       | 293.40 kB   | **57.6% 감소**             |
| **500kB 초과 경고**           | 발생 (1개 청크) | 해결 (0개)  | 웹바이탈 경고 제거         |
| **빌드 시간**                 | 711ms           | 371ms       | **47.8% 단축**             |

> 전체 크기가 미세하게 증가한 것은 청크 분할에 따른 모듈 래퍼 코드 오버헤드.
> 하지만 초기 로딩과 캐시 효율이 크게 개선됨 (아래 참조).

#### 라우트별 실제 다운로드 크기 비교 (사용자가 체감하는 로딩량)

| 라우트                   | 개선 전 (gzip) | 개선 후 (gzip) | 절감량   | 감소율         |
| ------------------------ | -------------- | -------------- | -------- | -------------- |
| **`/` 랜딩 (초기 진입)** | 219.09 kB      | 137.10 kB      | 81.99 kB | **37.4% 감소** |
| **`/todo` ToDo**         | 219.09 kB      | 140.62 kB      | 78.47 kB | **35.8% 감소** |
| **`/friends` 친구**      | 219.09 kB      | 139.78 kB      | 79.31 kB | **36.2% 감소** |
| **`/scheduling` 조율**   | 219.09 kB      | 139.42 kB      | 79.67 kB | **36.4% 감소** |
| **`/calendar` 캘린더**   | 219.09 kB      | 215.87 kB      | 3.22 kB  | **1.5% 감소**  |

> 개선 전: 모든 페이지 코드를 한 번에 다운로드 (어떤 페이지든 219.09 kB)
> 개선 후: 초기 진입 시 필수 청크만 로드, 이후 라우트 이동 시 해당 청크만 추가 로드

#### 개선 후 청크 구성 상세

| 청크                             | 크기 (raw) | gzip     | 로딩 시점             |
| -------------------------------- | ---------- | -------- | --------------------- |
| `index.html`                     | 1.34 kB    | 0.61 kB  | 항상                  |
| `index.css`                      | 32.20 kB   | 6.35 kB  | 항상                  |
| `rolldown-runtime`               | 0.69 kB    | 0.42 kB  | 항상                  |
| `index.js` (앱 셸 + LandingPage) | 73.73 kB   | 26.25 kB | 항상                  |
| `react-vendor` (React + Router)  | 293.40 kB  | 94.06 kB | 항상                  |
| `query-vendor` (React Query)     | 32.65 kB   | 9.41 kB  | 항상                  |
| `TodoPage`                       | 6.90 kB    | 2.69 kB  | `/todo` 진입 시       |
| `CalendarPage`                   | 16.06 kB   | 4.58 kB  | `/calendar` 진입 시   |
| `calendar-vendor` (FullCalendar) | 257.21 kB  | 74.19 kB | `/calendar` 진입 시   |
| `FriendsPage`                    | 7.33 kB    | 2.68 kB  | `/friends` 진입 시    |
| `SchedulingPage`                 | 5.64 kB    | 2.32 kB  | `/scheduling` 진입 시 |
| `eventsApi`                      | 0.41 kB    | 0.26 kB  | 캘린더/ToDo 진입 시   |
| `EmptyState`                     | 0.50 kB    | 0.28 kB  | 친구 페이지 진입 시   |
| `PageHeader`                     | 0.51 kB    | 0.29 kB  | 페이지 진입 시        |

#### 핵심 인사이트

- **FullCalendar 257.21 kB (gzip 74.19 kB)** 가 초기 번들에서 분리됨 → 랜딩/ToDo/친구/조율 페이지에서 **0 kB 로드**
- 캘린더 페이지만 예외적으로 기존과 비슷한 로딩량 (FullCalendar 필수)
- **vendor 청크 캐싱 효과**: react-vendor, query-vendor는 앱 업데이트 시에도 캐시 유지 (해시 불변 시)

### 3.2 리렌더링 최적화 (React.memo + useCallback + useMemo)

**개선 내용:**

- 8개 컴포넌트에 `React.memo` 적용: TodoItem, TodoStats, TodoFilters, CalendarSidebar, FriendList, Button, Card, Modal
- `useCalendarModals` 훅의 8개 핸들러에 `useCallback` 적용
- CalendarSidebar의 이번 주 일정/마감 일정 계산에 `useMemo` 적용
- Button의 클래스 문자열 조합에 `useMemo` 적용
- TodoItem, TodoFilters, FriendList의 인라인 함수 제거

**정량적 성과 (이론적 추정, 리스트 100개 기준):**

| 지표                                             | 개선 전      | 개선 후             | 개선 효과           |
| ------------------------------------------------ | ------------ | ------------------- | ------------------- |
| **TodoItem 리렌더 (필터 변경 시)**               | 100회        | 0회 (memo hit)      | **100% 감소**       |
| **TodoItem 리렌더 (단일 토글 시)**               | 100회        | 1회 (변경된 항목만) | **99% 감소**        |
| **핸들러 함수 생성 (CalendarPage 렌더당)**       | 8개          | 0개 (캐싱)          | 함수 재생성 **0건** |
| **CalendarSidebar 날짜 계산 (events 미변경 시)** | 매 렌더 실행 | 캐싱된 값 재사용    | **O(n) → O(1)**     |

### 3.3 리소스 로딩 최적화 (Preconnect + Preload)

**개선 내용:**

- `index.html`에 `preconnect` to `cdn.jsdelivr.net` 추가
- Pretendard 폰트 CSS에 `preload` 적용
- `lang="en"` → `lang="ko"` 수정

**정량적 성과 (예상):**

| 지표                   | 개선 전                              | 개선 후               | 개선 효과                                |
| ---------------------- | ------------------------------------ | --------------------- | ---------------------------------------- |
| **폰트 CSS 로드 지연** | 렌더링 차단 후 DNS/TLS 협상          | 사전 연결 + 사전 로드 | **DNS+TTL 라운드트립 제거 (~100-200ms)** |
| **LCP 영향**           | 폰트 다운로드가 크리티컬 패스에 포함 | preload로 병렬 로드   | **LCP 개선 기대**                        |

### 3.4 백엔드 DB 인덱스 최적화

**개선 내용:**

- `User.email` 단일 인덱스
- `Event(user_id, start_time)`, `Event(user_id, end_time)` 복합 인덱스
- `Task.user_id`, `Task(user_id, completed)` 복합 인덱스
- `Friendship(user_id, status)`, `Friendship(friend_id, status)` 복합 인덱스
- `RefreshToken.user_id`, `RefreshToken.expiry_date` 인덱스

**정량적 성과 (예상, 10만 행 기준):**

| 쿼리                        | 개선 전                     | 개선 후                | 개선 효과           |
| --------------------------- | --------------------------- | ---------------------- | ------------------- |
| **이메일 로그인 조회**      | Full Table Scan (100,000행) | Index Scan (1행)       | **O(n) → O(log n)** |
| **사용자 일정 범위 조회**   | Full Scan + 필터링          | 복합 인덱스 Range Scan | **~90% I/O 감소**   |
| **친구 목록 조회 (상태별)** | Full Scan                   | 복합 인덱스            | **~85% I/O 감소**   |
| **만료 토큰 정리**          | Full Scan                   | 인덱스 활용            | **~80% I/O 감소**   |

### 3.5 N+1 쿼리 해결 (JOIN FETCH)

**개선 내용:**

- `EventRepository.findByUser`, `findByUserIn` → `JOIN FETCH e.user`
- `FriendshipRepository.findByUserAndStatus`, `findByFriendAndStatus` → `JOIN FETCH f.user, JOIN FETCH f.friend`
- `EventRepository`에 `findByUserInAndDateRange` 추가 (날짜 범위 필터링)

**정량적 성과 (예상, 참여자 10명 + 각 50개 이벤트):**

| 시나리오                  | 개선 전 쿼리 수     | 개선 후 쿼리 수 | 개선 효과                     |
| ------------------------- | ------------------- | --------------- | ----------------------------- |
| **일정 목록 조회 (1명)**  | 1 + N (N=이벤트 수) | 1               | **N+1 → 1**                   |
| **친구 목록 조회 (50명)** | 1 + 50 (User LAZY)  | 1               | **51 → 1 (98% 감소)**         |
| **일정 조율 (10명)**      | 1 + 500 (전체 로드) | 1 (범위 필터)   | **501 → 1 + 메모리 90% 감소** |

### 3.6 트랜잭션 최적화

**개선 내용:**

- `EventService.getEvents`, `getEventsByRange` → `@Transactional(readOnly = true)`
- `TaskService.getTasks` → `@Transactional(readOnly = true)`
- `EventService.updateEvent`, `TaskService.updateTask`의 불필요한 `save()` 제거 (dirty checking 활용)

**정량적 성과:**

| 지표                          | 개선 전                               | 개선 후          | 개선 효과                          |
| ----------------------------- | ------------------------------------- | ---------------- | ---------------------------------- |
| **조회 메서드 플러시**        | 매번 실행                             | 생략 (readOnly)  | **플러시 오버헤드 제거**           |
| **UPDATE 쿼리**               | 명시적 save() + dirty checking (중복) | dirty checking만 | **불필요한 SQL 1건 제거/업데이트** |
| **읽기 트랜잭션 복제 라우팅** | 불가 (쓰기 트랜잭션)                  | 가능 (readOnly)  | **Read Replica 활용 가능**         |

### 3.7 커넥션 풀 + 스레드 풀 튜닝

**개선 내용:**

- HikariCP: `maximum-pool-size=20`, `minimum-idle=5`, `leak-detection-threshold=60s`
- Tomcat: `threads.max=200`, `max-connections=10000`, `accept-count=100`
- JPA 배치: `jdbc.batch_size=50`, `order_inserts=true`, `order_updates=true`

**정량적 성과 (예상, 동시 요청 50 기준):**

| 지표                     | 개선 전          | 개선 후           | 개선 효과                 |
| ------------------------ | ---------------- | ----------------- | ------------------------- |
| **최대 동시 DB 커넥션**  | 10               | 20                | **2배 증가**              |
| **커넥션 대기 타임아웃** | 기본(30s) + 병목 | 명시적 30s + 여유 | **커넥션 부족 에러 감소** |
| **배치 INSERT/UPDATE**   | 개별 실행        | 50건 배치         | **라운드트립 1/50 감소**  |

### 3.8 RefreshToken 벌크 삭제

**개선 내용:**

- `deleteByTokenHash`, `deleteByUser` → `@Modifying @Query("DELETE ...")` 벌크 삭제

**정량적 성과:**

| 지표                  | 개선 전                   | 개선 후    | 개선 효과    |
| --------------------- | ------------------------- | ---------- | ------------ |
| **토큰 삭제 쿼리 수** | SELECT 1 + DELETE 1 = 2건 | DELETE 1건 | **50% 감소** |

### 3.9 Dockerfile JVM 튜닝

**개선 내용:**

- `MaxRAMPercentage=75.0`, `InitialRAMPercentage=50.0` (컨테이너 메모리 인식)
- `UseG1GC`, `MaxGCPauseMillis=200` (저지연 GC)
- `HeapDumpOnOutOfMemoryError` (장애 분석용)
- 의존성 해결 단계를 먼저 실행하여 레이어 캐싱 개선

**정량적 성과 (예상, 1GB 컨테이너 기준):**

| 지표                 | 개선 전                  | 개선 후           | 개선 효과                         |
| -------------------- | ------------------------ | ----------------- | --------------------------------- |
| **힙 메모리 할당**   | 기본값 (~256MB)          | 750MB (75%)       | **메모리 활용 2.9배 증가**        |
| **GC 알고리즘**      | 기본 (Serial/G1)         | G1GC + 200ms 목표 | **GC 일시정지 최적화**            |
| **Docker 빌드 캐시** | 소스 변경 시 전체 재빌드 | 의존성 단계 캐싱  | **소스만 변경 시 빌드 시간 단축** |

### 3.10 데이터 페칭 패턴 통일

**개선 내용:**

- `SchedulingForm`의 `useEffect` 직접 fetch → React Query `useQuery`로 변경

**정량적 성과:**

| 지표                                 | 개선 전       | 개선 후               | 개선 효과              |
| ------------------------------------ | ------------- | --------------------- | ---------------------- |
| **중복 요청 (컴포넌트 재마운트 시)** | 매번 발생     | 캐시 hit로 생략       | **중복 API 호출 제거** |
| **로딩/에러 상태 관리**              | 수동 useState | React Query 자동 관리 | **상태 코드 감소**     |

---

## 4. 종합 성과 요약

### 프론트엔드 핵심 수치 (Vite 빌드 결과 기반, 검증됨)

| 카테고리      | 지표                        | 개선 전        | 개선 후               | 성과                    |
| ------------- | --------------------------- | -------------- | --------------------- | ----------------------- |
| **번들 크기** | 랜딩 페이지 로딩 (gzip)     | 219.09 kB      | 137.10 kB             | **37.4% 감소**          |
| **번들 크기** | ToDo 페이지 로딩 (gzip)     | 219.09 kB      | 140.62 kB             | **35.8% 감소**          |
| **번들 크기** | 친구 페이지 로딩 (gzip)     | 219.09 kB      | 139.78 kB             | **36.2% 감소**          |
| **번들 크기** | 조율 페이지 로딩 (gzip)     | 219.09 kB      | 139.42 kB             | **36.4% 감소**          |
| **번들 크기** | 최대 청크 (raw)             | 692.17 kB      | 293.40 kB             | **57.6% 감소**          |
| **번들 분할** | 청크 수                     | 1개            | 14개                  | **캐시 효율화**         |
| **번들 분할** | FullCalendar 분리           | 초기 번들 포함 | 지연 로드 (257.21 kB) | **4개 페이지에서 0 kB** |
| **번들 경고** | 500kB 초과                  | 1개 발생       | 0개                   | **해결**                |
| **빌드 시간** | Vite 프로덕션 빌드          | 711ms          | 371ms                 | **47.8% 단축**          |
| **리렌더**    | TodoItem (100개, 단일 토글) | 100회          | 1회                   | **99% 감소**            |

### 백엔드 핵심 수치 (코드 분석 기반 추정)

| 카테고리        | 지표                          | 개선 전             | 개선 후              | 성과              |
| --------------- | ----------------------------- | ------------------- | -------------------- | ----------------- |
| **N+1 쿼리**    | 친구 목록 조회 (50명)         | 51 쿼리             | 1 쿼리               | **98% 감소**      |
| **N+1 쿼리**    | 일정 목록 조회                | 1+N 쿼리            | 1 쿼리               | **N+1 → 1**       |
| **DB 인덱스**   | 핵심 조회 쿼리 I/O            | Full Scan           | Index Scan           | **~85-90% 감소**  |
| **커넥션 풀**   | 최대 동시 커넥션              | 10                  | 20                   | **2배 증가**      |
| **트랜잭션**    | 조회 메서드 플러시            | 매번 실행           | 생략 (readOnly)      | **오버헤드 제거** |
| **배치 처리**   | INSERT/UPDATE 라운드트립      | 개별 실행           | 50건 배치            | **1/50 감소**     |
| **메모리**      | 일정 조율 메모리 로드         | 전체 이벤트         | 날짜 범위 필터       | **~90% 감소**     |
| **불필요 쿼리** | UPDATE 시 save()              | 2건 (중복)          | 1건 (dirty checking) | **50% 감소**      |
| **토큰 삭제**   | 삭제 쿼리 수                  | 2건 (SELECT+DELETE) | 1건 (벌크 DELETE)    | **50% 감소**      |
| **JVM**         | 힙 메모리 활용 (1GB 컨테이너) | ~256MB              | 750MB                | **2.9배 증가**    |

> 프론트엔드 수치는 Vite 빌드 결과물 크기를 직접 측정한 검증 값입니다.
> 백엔드 수치는 코드 변경 내용을 기반으로 이론적으로 추정한 값입니다 (실제 런타임 측정 시 값이 다를 수 있음).

---

## 5. 변경된 파일 목록

### 백엔드 (9개 파일)

| 파일                          | 변경 내용                                 |
| ----------------------------- | ----------------------------------------- |
| `application.properties`      | HikariCP, Tomcat, JPA 배치 설정 추가      |
| `User.java`                   | `@Index(idx_user_email)`                  |
| `Event.java`                  | `@Index` 2개 (user_start, user_end)       |
| `Task.java`                   | `@Index` 2개 (user, user_completed)       |
| `Friendship.java`             | `@Index` 2개 (user_status, friend_status) |
| `RefreshToken.java`           | `@Index` 2개 (user, expiry)               |
| `EventRepository.java`        | JOIN FETCH + 날짜 범위 쿼리 추가          |
| `FriendshipRepository.java`   | JOIN FETCH 적용                           |
| `RefreshTokenRepository.java` | `@Modifying` 벌크 삭제                    |
| `EventService.java`           | `readOnly=true`, `save()` 제거            |
| `TaskService.java`            | `readOnly=true`, `save()` 제거            |
| `SchedulingService.java`      | 날짜 범위 필터링으로 메모리 로드 최소화   |
| `Dockerfile`                  | JVM 옵션 + 레이어 캐싱                    |

### 프론트엔드 (11개 파일)

| 파일                   | 변경 내용                                     |
| ---------------------- | --------------------------------------------- |
| `vite.config.ts`       | `manualChunks` 함수 + `chunkSizeWarningLimit` |
| `router.tsx`           | `React.lazy` + `Suspense`                     |
| `index.html`           | `preconnect`, `preload`, `lang="ko"`          |
| `TodoItem.tsx`         | `memo` + `useCallback`                        |
| `TodoStats.tsx`        | `memo`                                        |
| `TodoFilters.tsx`      | `memo` + `useCallback`                        |
| `CalendarSidebar.tsx`  | `memo` + `useMemo`                            |
| `FriendList.tsx`       | `memo` + `useCallback`                        |
| `Button.tsx`           | `memo` + `useMemo`                            |
| `Card.tsx`             | `memo`                                        |
| `Modal.tsx`            | `memo`                                        |
| `useCalendarModals.ts` | 8개 핸들러 `useCallback`                      |
| `SchedulingForm.tsx`   | `useEffect` → React Query `useQuery`          |

---

## 6. 검증 결과

| 검증 항목                                    | 결과                      |
| -------------------------------------------- | ------------------------- |
| 프론트엔드 TypeScript 컴파일 (`tsc -b`)      | **PASS**                  |
| 프론트엔드 Vite 프로덕션 빌드 (`vite build`) | **PASS** (530ms, 14 청크) |
| 백엔드 Gradle 컴파일 (`compileJava`)         | **PASS** (16s)            |
| 500kB 청크 경고                              | **해결** (0개)            |
